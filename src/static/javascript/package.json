{
  "name": "duty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:meeting": "parcel src/meeting/index.js -d dist/meeting",
    "watch:members": "parcel src/members/index.js -d dist/members",
    "watch:standup": "parcel src/standup/index.js -d dist/standup",
    "watch:poker": "parcel src/poker/index.js -d dist/poker",
    "watch": "npm-run-all --parallel watch:meeting watch:members watch:standup watch:poker",
    "build:meeting": "parcel build src/meeting/index.js -d dist/meeting",
    "build:members": "parcel build src/members/index.js -d dist/members",
    "build:standup": "parcel build src/standup/index.js -d dist/standup",
    "build:standup": "parcel build src/standup/index.js -d dist/standup",
    "build": "npm-run-all --parallel build:meeting build:members build:standup build:poker",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-plugin-transform-regenerator": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1",
    "mithril": "^1.1.6",
    "npm-run-all": "^4.1.2",
    "parcel-bundler": "^1.3.1",
    "postcss-modules": "^1.1.0"
  }
}
